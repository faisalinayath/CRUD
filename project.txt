public IActionResult Advertise(int? id)
        {
            var property=_db.Property.Find(id);
            System.Console.WriteLine(property);

            _db.Ads.Add(property);
            _db.SaveChanges();
            return View();

        }
