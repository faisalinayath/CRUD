<div style="margin-left:50px; padding-top:20px; font-family:'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif; font-size:30px;">
    <p>
        <u>Recently Added Properties</u>
    </p>
</div>

<div id="carouselExampleCaptions" class="carousel carousel-dark slide" data-bs-ride="false">
    <div class="carousel-indicators" style="margin-bottom:0">
        @{

            // Get the property items from your SQL database
            var propertyItems = GetPropertyItemsFromDatabase();

            for (var i = 0; i < propertyItems.Count; i++)
            {
                <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="@i" class="@(i == 0 ? "active" : "")" aria-current="@(i == 0 ? "true" : "")" aria-label="Slide @(i + 1)"></button>
            }
        }
    </div>
    <div class="carousel-inner">
        @for (var i = 0; i < propertyItems.Count; i++)
        {
            <div class="carousel-item @(i == 0 ? "active" : "")">
                <div class="card-group">
                    @foreach (var propertyItem in propertyItems)
                    {
                        <div class="card shadow mb-5 rounded">
                            <img class="card-img-top" src="@(propertyItem.ImageUrl)" alt="Card image cap">
                            <div class="card-body">
                                <h3>@(propertyItem.Title)</h3>
                                <p>@(propertyItem.Location)</p>
                                <p>Rs. @(propertyItem.Price)</p>
                                <div>
                                    <button type="button" class="btn btn-primary mt-3" style="border-radius:50px;"><b>View</b></button>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        }
    </div>
    <a class="carousel-control-prev" href="#carouselExampleCaptions" role="button" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleCaptions" role="button" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>
